# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'First demo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for First demo
  pod 'Alamofire', '4.4.0'
  pod 'Kingfisher'
  target 'First demoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'First demoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
